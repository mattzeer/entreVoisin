@using EntreVoisin.Models;
@{
    ViewBag.Title = "Types";
}

<h2>Types</h2>

<div class="row well col-lg-12">

    <ul class="nav nav-tabs" id="tabs">
        <li class="active"><a href="#tabConfirmation" data-toggle="tab">Type confirmation</a></li>
        <li><a href="#tabObjet" data-toggle="tab">Type d'Objet</a></li>
        <li><a href="#tabService" data-toggle="tab">Type de service</a></li>
        <li><a href="#tabActus" data-toggle="tab">Type d'actualité</a></li>
    </ul>
    <div class="tab-content">
        <div class="tab-pane fade in active" id="tabConfirmation">
            <br />
            <table class="table">
                <thead>
                    <tr>
                        <th>Code</th>
                        <td>Libelle</td>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <th>l</th>
                        <td>l</td>
                    </tr>
                </tbody>
            </table>
            <hr />
            <div class="row">
                <div class="col-lg-12">
                    @using (Html.BeginForm("", "", FormMethod.Post, new { @class = "navbar-form" }))
                    {
                        <div class="form-group">
                            @Html.TextBox("test", "", new { @class = "form-control", placeholder = "Code du type" })
                        </div>
                        <div class="form-group col-lg-offset-1">
                            @Html.TextBox("test", "", new { @class = "form-control", placeholder = "Libellé du type" })
                        </div>
                        <div class="form-group col-lg-offset-1">
                            <button type="submit" class="btn btn-primary">Ajouter le Type</button>
                        </div>

                    }
                </div>
            </div>
        </div>

        <div class="tab-pane fade" id="tabService">
            <br />
            <table class="table">
                <thead>
                    <tr>
                        <th>Code</th>
                        <td>Libelle</td>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <th>l</th>
                        <td>l</td>
                    </tr>
                </tbody>
            </table>
            <hr />
            <div class="row">
                <div class="col-lg-12">
                    @using (Html.BeginForm("", "", FormMethod.Post, new { @class = "navbar-form" }))
                    {
                        <div class="form-group">
                            @Html.TextBox("test", "", new { @class = "form-control", placeholder = "Code du type" })
                        </div>
                        <div class="form-group col-lg-offset-1">
                            @Html.TextBox("test", "", new { @class = "form-control", placeholder = "Libellé du type" })
                        </div>
                        <div class="form-group col-lg-offset-1">
                            <button type="submit" class="btn btn-primary">Ajouter le Type</button>
                        </div>

                    }
                </div>
            </div>

        </div>

        <div class="tab-pane fade" id="tabObjet">
            <br />
            <table class="table">
                <thead>
                    <tr>
                        <th>Code</th>
                        <td>Libelle</td>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <th>l</th>
                        <td>l</td>
                    </tr>
                </tbody>
            </table>
            <hr />
            <div class="row">
                <div class="col-lg-12">
                    @using (Html.BeginForm("", "", FormMethod.Post, new { @class = "navbar-form" }))
                    {
                        <div class="form-group">
                            @Html.TextBox("test", "", new { @class = "form-control", placeholder = "Code du type" })
                        </div>
                        <div class="form-group col-lg-offset-1">
                            @Html.TextBox("test", "", new { @class = "form-control", placeholder = "Libellé du type" })
                        </div>
                        <div class="form-group col-lg-offset-1">
                            <button type="submit" class="btn btn-primary">Ajouter le Type</button>
                        </div>

                    }
                </div>
            </div>
        </div>

        <div class="tab-pane fade" id="tabActus">
            <br />
            <table class="table">
                <thead>
                    <tr>
                        <th>Code</th>
                        <td>Libelle</td>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <th>l</th>
                        <td>l</td>
                    </tr>
                </tbody>
            </table>
            <hr />
            <div class="row">
                <div class="col-lg-12">
                    @using (Html.BeginForm("", "", FormMethod.Post, new { @class = "navbar-form" }))
                    {
                        <div class="form-group">
                            @Html.TextBox("test", "", new { @class = "form-control", placeholder = "Code du type" })
                        </div>
                        <div class="form-group col-lg-offset-1">
                            @Html.TextBox("test", "", new { @class = "form-control", placeholder = "Libellé du type" })
                        </div>
                        <div class="form-group col-lg-offset-1">
                            <button type="submit" class="btn btn-primary">Ajouter le Type</button>
                        </div>

                    }
                </div>
            </div>
        </div>
    </div>
</div>